name: Update Big Trees forecast

on:
  schedule:
    - cron: "0 */8 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: update-big-trees-forecast
  cancel-in-progress: false

jobs:
  update-forecast:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main

      - name: Sync to latest main
        run: |
          git fetch origin main
          git checkout main
          git reset --hard origin/main

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Update forecast JSON
        env:
          BIG_TREES_FORECAST_REQUIRE_CORE_RANGES: "1"
          BIG_TREES_FORECAST_REQUIRE_EXTENDED_RANGES: "0"
        run: |
          chmod +x scripts/update_big_trees_forecast.sh
          scripts/update_big_trees_forecast.sh

      - name: Validate forecast asset freshness
        run: |
          python3 scripts/check_forecast_assets.py \
            --streamflow assets/data/forecasts/big_trees_latest.json \
            --max-age-hours 36

      - name: Publish forecast asset to live-data branch
        env:
          LIVE_DATA_COMMIT_MESSAGE: "chore: update Big Trees forecast data"
          LIVE_DATA_MAX_AGE_HOURS: "36"
        run: |
          scripts/publish_live_data_artifacts.sh \
            assets/data/forecasts/big_trees_latest.json
